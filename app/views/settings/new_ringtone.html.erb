<div id="content">
  <% if flash[:notice] -%>
    <div id="flash">
      <div style="margin-bottom:0px;">
        <%= flash[:notice] %>
      </div>
    </div>
  <% end -%>

  <div id="new_ringtone">
    <h2><%= link_to "Settings", settings_path %> > New ringtone&nbsp;&nbsp;
      <%= image_tag("spinner.gif",:align => "absmiddle",
        :border => 0,:id => "spinner",
        :style =>"display: none;" ) %></h2>
    <% if !@ringtone.nil? %>
      <ul>
        <% @ringtone.errors.each_with_index do |msg, i| %>
          <li><%= msg[1] %></li>
        <% end %>
      </ul>
    <% end %>

    <%= form_for :ringtone, :html => {:multipart => true } do |f| -%>
      <table>
        <tbody>
          <tr>
          <tr>
            <td>Song title : </td>
            <td><%= f.text_field :song_title, :size => 20 %></td>
          </tr>

          <tr>
            <td>Artist name : </td>
            <td><%= f.text_field :artist_name, :size => 20 %></td>
          </tr>

          <tr>
            <td>File : </td>
            <td><%= f.file_field :f_name %></td>
          </tr>

          <tr>
            <td>
              <a class="submit"><%= submit_tag "Save", :name => "save_ringtone" %></a>
            </td>
          </tr>
          </tr>
        </tbody>
      </table>
    <% end -%>

  </div>
</div>
